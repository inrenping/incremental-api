# Supabase Supavisor pooler（事务模式，端口 6543）
# 注意：事务模式不支持 prepared statements，客户端必须设置 prepare: false
# 本地开发请复制为 .env（vercel dev 只读取 .env，不读取 .env.local）
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-1-<region>.pooler.supabase.com:6543/postgres
